using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//What I want
//Node class
//-Each node has a
//Tile tile;
//Node Parent;
//H value ->euc distance to destination
//G value ->movement cost : 1
//HG value -> H+G


//Tile methods
//aTileObject.getRow();
//aTileObject.getColumn();
/*public class Astar : MonoBehaviour {
	static List<Node> openList;
	static List<Node> closedList;


	//Not Recursive. Contains recursive algorithm.
	static List<Tile> getAStarPath(List<Tiles> allTheTiles, Tile start, Tile dest)//Return a list of tiles with best path.
	{//Set up list of nodes with tiles inside them
		
	}
	static Node dest 
	static int getEucDist(Tile a, Tile b){//Not really Euc distance since not square rooted before return.
		int x1 = a.getColumn (); x2 = b.getColumn ();
		int y1 = a.getRow (); y2 = b.getColumn ();
		return ((x1 - x2) ^ 2 + (y1 - y2) ^ 2);
	}
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}*/
